import java.util.InputMismatchException;
import java.util.Scanner;

public class Quiz {
    public static Scanner sc = new Scanner(System.in);
    public static void printName() {
        try {
            int input = sc.nextInt();
            sc.nextLine();
        } catch (InputMismatchException ex) {
            System.out.println("The error is " + ex);
            try {
                String input = sc.nextLine();
                int i = Integer.parseInt(input);
            } catch (InputMismatchException e) {
                System.out.println("The error is " + e);
            }
        }
    }
}

Input "1"  and "a" , what are the output?
A. Process finished with exit code 0
b. The error is java.util.InputMismatchException and java.lang.NumberFormatException: For input string "a"(answer)
c. The error is java.util.InputMismatchException