import java.io.IOException

public class Quiz {
    public static void main(String[] args) {
        public static void main(String[] args) {
            try {
                FileInputStream file = new FileInputStream("non_existent_file.txt");
                int data = file.read();
                file.close();
            } catch (IOException e) {
                System.out.println("IOException occurred: " + e.getMessage());
            } catch (Exception e) {
                System.out.println("Exception occurred: " + e.getMessage());
            }finally {
                System.out.println("a = " + a);
                System.out.println("b = " + b);
            }
        }
    }
}

Below is a snippet of Java code, look at it and tell whether this code uses an unchecked exception class or checked exception class:
A. unchecked exception class
B. checked exception class
C. Both of them (answer)
D. Not using